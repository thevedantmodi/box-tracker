# Supabase project URL — found in Project Settings > API
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous/public key — found in Project Settings > API
SUPABASE_ANON_KEY=your-anon-key-here

# PowerSync instance URL — found in PowerSync dashboard after linking to Supabase
# Filled in during Task 03 (Core Data Layer)
POWERSYNC_URL=https://your-instance.powersync.journeyapps.com
